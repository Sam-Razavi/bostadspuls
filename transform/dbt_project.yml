name: bostadspuls
version: "1.0.0"
config-version: 2

profile: bostadspuls

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bostadspuls:
    staging:
      +materialized: view
      +dataset: bostadspuls_staging
    intermediate:
      +materialized: view
      +dataset: bostadspuls_staging
    marts:
      +materialized: table
      +dataset: bostadspuls_marts
